// Declare the variable without assigning it a value
let age;

// Declare the variable and assign it an initial value of 5
let level = 5;

console.log(age); // This will be undefined
// Undefined is a special data type in JavaScript, meaning "this variable's value has not yet been defined"

console.log(level); // This will be 5